# Architecture

<!-- Filled in progressively as phases land. Sections below are the intended scope. -->

## Overview

## Pipeline stages (Stage 1 extraction, Stage 2 LLM compilation, Stage 3 TTS + sync)

## Backend serving model (offline-first package distribution)

## Frontend architecture (state management, playback/highlight sync)

## Why these choices (Chatterbox vs alternatives, marker-pdf vs alternatives, per-segment sync vs forced alignment)
