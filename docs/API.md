# Backend API

<!-- Filled in during Phase 4 to match the actual FastAPI routes exactly. -->

## `GET /health`

## `GET /books`

## `GET /books/{book_id}/manifest`

## `GET /books/{book_id}/download`

## `GET /books/{book_id}/assets/{path}`
